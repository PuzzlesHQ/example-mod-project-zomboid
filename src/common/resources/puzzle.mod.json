{
  "formatVersion": 3,
  "display-name": "${p_name}",
  "id": "${p_id}",
  "version": "${p_version}",
  "description": "${p_desc}",
  "authors": [
    "${p_author}"
  ],
  "can-load-on": {
    "client": true,
    "server": true
  },

  "meta": {
    "icon": "${p_id}:icons/icon.png"
  },

  "entrypoints": {
    "client-postInit": "com.examplemod.exmod.ClientExampleMod",
    "client-init": "com.examplemod.exmod.ClientExampleMod",
    "client-preInit": "com.examplemod.exmod.ClientExampleMod",

    "preInit": "com.examplemod.exmod.PreInitExampleMod",
    "init": "com.examplemod.exmod.InitExampleMod",
    "postInit": "com.examplemod.exmod.PostInitExampleMod",

    "server-init": "com.examplemod.exmod.ServerExampleMod"
  },

  // Comments are supported in here, just ignore the error your code editor may display
  "mixins": [
    "example-mod.common.mixins.json",
    {
      "config": "example-mod.client.mixins.json",
      "environment": "client"
    }
  ],

  "depends": {
    "project-zomboid": ">=41.78.16",
    "puzzle-loader-core": ">=1.3.4-alpha",
    "puzzle-loader-zomboid": ">=1.0.0-alpha"
  },

  "access-writers": [
    "example-mod.manipulator"
  ],

  "custom": {
    "loom:injected_interfaces": {
    }
  }
}